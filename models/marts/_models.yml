version: 2

models:
  - name: fct_customer_orders
    description: "Customer-level order summary"
    columns:
      - name: customer_id
        tests:
          - unique
          - not_null
      - name: total_orders
        tests:
          - not_null
      - name: total_amount
        tests:
          - not_null